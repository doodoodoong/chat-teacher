import { extendTheme } from '@chakra-ui/react';
import './index.css';
const theme = extendTheme({
  fonts: {
    body: `'Poppins', sans-serif`,
    mono: `'NeoDunggeunmoPro-Regular', sans-serif`,
  },
});

export default theme;
